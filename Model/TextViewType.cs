
namespace Model
{
    public enum TextViewType
    {
        BottomRadius,
        BottomThickness,
        Height,
        TopRadius,
        TopThickness,
        TopWidth,
        WallThickness
    }
}
