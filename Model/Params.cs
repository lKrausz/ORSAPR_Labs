
namespace Model
{
    public enum Params
    {
        BottomRadius,
        BottomThickness,
        Height,
        TopRadius,
        TopThickness,
        TopWidth,
        WallThickness
    }
}
